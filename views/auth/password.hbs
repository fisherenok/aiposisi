
<div class="row">
    <div id="login" class="col s6 offset-s3">
        {{#if error}}
            <p class="alert">{{error}}</p>
        {{/if}}
        <h1 class="h-auth">Задать новый пароль</h1>
        <form action="/auth/password" method="POST">
            <div class="input-field">
                <input id="password" name="password" type="password" class="validate" required>
                <label for="password">Новый пароль</label>
                <span class="helper-text" data-error="Введите пароль"></span>
            </div>

            <div class="input-field">
                <input id="passwordTwo" name="passwordTwo" type="password" class="validate" required>
                <label for="passwordTwo">Новый пароль</label>
                <span class="helper-text" data-error="Введите пароль"></span>
            </div>

            <input type="hidden" name="_csrf" value="{{csrf}}">
            <input type="hidden" name="userId" value="{{userId}}">
            <input type="hidden" name="token" value="{{token}}">

            <button type="submit" class="btn btn-primary btn-auth">Обновить пароль</button>
        </form>
    </div>

</div>